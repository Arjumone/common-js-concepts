/**
 * 8(eight) way to get undefined
 * 1. variable that is not initialized will give undefined
 * 2. function with no return than get undefined
 * 3. parameter that is not passed that will be undefined
 * 4. if return has nothing on the right side will return undefined
 * 5.property that does not exists  on an object will give undefined
 * 6. accessing array elements outside of the index range
 * 7.deleting an element inside an array(do not do it.you should use splice)
 * 8. set a value directly to undefined
 * */ 